This is just a simple test file to be put into a ramdisk image by rampak. If everything goes well, we should be able to slap that image onto the kernel with kpkg, modify the kernel to load it from the filesystem and print out this message. Happy sailing!

    --Me
    
